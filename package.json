{
  "name": "euler-project",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm --prefix react-interface run start\" \"npm --prefix flask-backend run start\"",
    "build": "npm --prefix react-interface run build",
    "install-all": "npm install && npm --prefix react-interface install && npm --prefix flask-backend install"
  },
  "devDependencies": {
    "@types/axios": "^0.9.36",
    "@types/cheerio": "^0.22.35",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "cheerio": "^1.0.0"
  }
}
